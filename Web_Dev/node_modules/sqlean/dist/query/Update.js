"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
exports.Update = exports.UpdateData = void 0;
var Db_1 = require("../Db");
var Util_1 = require("../Util");
var Where_1 = require("./Where");
var UpdateData = /** @class */ (function () {
    function UpdateData() {
    }
    return UpdateData;
}());
exports.UpdateData = UpdateData;
function Update(data) {
    data = __assign(__assign({}, new UpdateData), data);
    var sqlSet = '';
    var updateValues = {};
    Object.keys(data.columnValues).forEach(function (key, i, arr) {
        var updateKey = '__update_' + key;
        sqlSet += Util_1.Util.toSnakeCase(key) + " = @" + updateKey + " " + (i !== arr.length - 1 ? ',' : '');
        updateValues[updateKey] = data.columnValues[key];
    });
    var whereResults = new Where_1.WhereBuilder(data.where || '').getResult();
    var sql = "\n        UPDATE " + data.table + "\n        SET " + sqlSet + "\n        " + whereResults.sql + "\n    ";
    // For Debug
    //console.log(sql);
    Db_1.DB._db.prepare(sql).run(__assign(__assign({}, updateValues), whereResults.params));
}
exports.Update = Update;
