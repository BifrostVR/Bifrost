import { ColumnValue } from "../Db";
import { Where } from "./Where";
export declare class UpdateData {
    table: string;
    columnValues: ColumnValue;
    where?: Where;
}
export declare function Update(data: UpdateData): void;
