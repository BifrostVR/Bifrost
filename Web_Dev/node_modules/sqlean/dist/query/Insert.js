"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
exports.Insert = exports.InsertData = void 0;
var Db_1 = require("../Db");
var Util_1 = require("../Util");
var InsertData = /** @class */ (function () {
    function InsertData() {
        this.rewrite = false;
    }
    return InsertData;
}());
exports.InsertData = InsertData;
function Insert(data) {
    data = __assign(__assign({}, new InsertData), data);
    var keys = Object.keys(data.columnValues);
    var dbNameKeys = keys.map(function (key) { return Util_1.Util.toSnakeCase(key); });
    var dbValueKeys = keys.map(function (key) { return '@' + key; });
    var sql = "\n        INSERT " + (data.rewrite ? 'OR REPLACE' : '') + "\n        INTO " + data.table + " (" + dbNameKeys.join(', ') + ")\n        VALUES (" + dbValueKeys.join(', ') + ")\n    ";
    // For Debug
    //console.log(sql);
    Db_1.DB._db.prepare(sql).run(data.columnValues);
}
exports.Insert = Insert;
